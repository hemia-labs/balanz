// Singleton identities are separated by capability; only their AppRole login is cached.
export const EFIRMA_VAULT_RUNTIME = Symbol('EFIRMA_VAULT_RUNTIME');
export const EFIRMA_VAULT_CLEANUP = Symbol('EFIRMA_VAULT_CLEANUP');
