# Instrucciones para Copilot

Lee y cumple `/AGENTS.md` antes de proponer o modificar código. Para cada archivo, aplica también
el `AGENTS.md` más cercano. Limita cada cambio al requisito actual, reutiliza patrones existentes
y no agregues dependencias ni abstracciones sin necesidad demostrable y aprobación explícita.
